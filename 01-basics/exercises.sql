-- 01-basics exercises. Write your query under each prompt and run it.
-- USE ecommerce; first.

-- 1. List all products with category_id = 2 (Books), cheapest first.
--    (Filtering by category *name* needs a JOIN to `categories` -- that's
--    02-querying. Here, category_id lives right on `products`.)


-- 2. Find the email addresses of every customer from Germany or France.


-- 3. Insert a new category called 'Garden'.


-- 4. Insert a product into that new category (any name/price you like).


-- 5. All products currently priced over 50.00, showing name and price only.


-- 6. Every order with status 'cancelled'.


-- 7. Update the 'Garden' category's name to 'Garden & Outdoor'.


-- 8. Delete the product you inserted in #4 (careful with WHERE).


-- 9. Count how many customers signed up in total (COUNT(*)).


-- 10. Show the 5 most recently placed orders (any status), most recent first.
